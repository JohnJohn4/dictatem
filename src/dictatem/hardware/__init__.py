"""Hardware Tier detection and resolution.

The pure ``HardwareTierResolver`` lives here; the native ``HardwareProbe``
adapter (``nvidia_probe``) is manual-QA only and excluded from pyright/tests.
"""
